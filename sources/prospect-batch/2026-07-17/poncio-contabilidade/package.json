{
  "name": "poncio-contabilidade-build",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "verify": "bash -c 'python3 -m http.server 43891 --bind 127.0.0.1 >/tmp/poncio-verify-server.log 2>&1 & pid=$!; trap \"kill $pid 2>/dev/null || true\" EXIT; sleep 1; node verify.mjs'"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
